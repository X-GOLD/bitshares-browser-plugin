var Apis = window.bitshares_ws;
let server = "wss://bitshares.openledger.info/ws";
// Apis.instance("wss://eu.nodes.bitshares.ws", true).init_promise.then(res => {
//     console.log("connected to:", res[0].network);
// });